; return 1 + 1;
2

; return 4 - 2;
2

; return 5 * 7;
35

; return 8 - 11;
-3

; return 12 / 6;
2

; return 12 / 7;
1

; return 11.0 - 5.5;
5.5

; return (2 + 2) * 3;
12

; return sqrt(4.0);
2.0

// Operator type errors raise, as in LambdaMOO.
; 1 + "a";
E_TYPE

; "a" - 1;
E_TYPE

; 1.5 * {};
E_TYPE

; 1 / "0";
E_TYPE

// Type mismatch is raised before the zero-divisor check (LambdaMOO order).
; "a" / 0;
E_TYPE

// Arithmetic is strict: both operands must have the same type (LambdaMOO).
; 1 + 1.5;
E_TYPE

; 1.5 - 1;
E_TYPE

; 1.5 * 2;
E_TYPE

; 2 ^ 2.0;
E_TYPE

// ...but a float may be raised to an integer power.
; return 3.5 ^ 4;
150.0625

// Ordering comparisons accept only like types, as in LambdaMOO.
; 1 < "a";
E_TYPE

; "a" <= 1;
E_TYPE

; 3 < 4.0;
E_TYPE

; {1} < {2};
E_TYPE

; true < false;
E_TYPE

; return 3 < 4;
1

; return #34 >= #32;
1

; return "foo" <= "Boo";
0

; return E_DIV > E_TYPE;
1
